---
name: Hot Potato CI

on:
  push:
    branches:
      - '!main'
  pull_request:
    branches:
      - main

jobs:
  build:
    uses: ./.github/workflows/build.yml

  # create-tag:
  #   runs-on: ubuntu-latest
  #   steps:
  #     - name: Create tag
  #       run: git tag -a ${{build.outputs.version}}

  # push-package:
  #   runs-on: ubuntu-latest
  #   steps:
  #     - name: Download artifacts
  #       uses: actions/download-artifacts@v3.0.2

  #     - name: NuGet push
  #       run: dotnet nuget push $GITHUB_WORKSPACE/nuget/**/*.nupkg -k ${{ secrets.NUGET_PUSH }} -s https://api.nuget.org/v3/index.json --skip-duplicate
  # create-release:
  #   runs-on: ubuntu-latest
  #   if: ${{ success() }}
  #   steps:
  #     - name: Create Release
  #       run: gh release create ${{ job.build.steps.gitversion.outputs.nuGetVersionV2 }}
        # uses: actions/create-release@v1.1.4
        # if: ${{ success() }}
        # env:
        #   GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }} # This token is provided by Actions, you do not need to create your own token
        # with:
        #   tag_name: ${{ steps.gitversion.outputs.nuGetVersionV2 }}
        #   release_name: ${{ steps.gitversion.outputs.nuGetVersionV2 }}
        #   draft: false
        #   prerelease: false
